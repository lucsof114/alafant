import { Metadata } from '@metaplex-foundation/mpl-token-metadata';
import { MintInfo } from '@solana/spl-token';
import { Connection, PublicKey } from '@solana/web3.js';
export declare function findMetadataAddress(mint: PublicKey, tokenProgramId: PublicKey): PublicKey;
type TokenMetadata = {
    publicKey: PublicKey;
    account: Metadata;
};
type TokenMintWithProgramId = {
    mint: PublicKey;
    programId: PublicKey;
};
interface RawMintWithProgramId extends MintInfo {
    programId: PublicKey;
}
export declare function fetchMintInfos(connection: Connection, mints: PublicKey[]): Promise<Array<[string, RawMintWithProgramId]>>;
export declare const removeEmptyChars: (value: string) => string;
export declare function fetchTokenMetadatas(connection: Connection, tokens: TokenMintWithProgramId[]): Promise<TokenMetadata[]>;
export {};
