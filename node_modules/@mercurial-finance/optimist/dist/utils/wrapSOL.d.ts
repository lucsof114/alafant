import { Blockhash, Connection, PublicKey, Transaction } from '@solana/web3.js';
interface CreateTxParams {
    connection: Connection;
    publicKey: PublicKey;
    blockhash: Blockhash;
    lastValidBlockHeight: number;
}
declare function createAndTransferWSOLTransaction({ connection, publicKey, blockhash, lastValidBlockHeight, amountToTransferInLamports, }: CreateTxParams & {
    amountToTransferInLamports: number;
}): Promise<Transaction>;
declare function createUnwrapSolTransaction({ connection, publicKey, blockhash, lastValidBlockHeight }: CreateTxParams): Promise<Transaction | undefined>;
export { createAndTransferWSOLTransaction, createUnwrapSolTransaction };
